apiVersion: v1
kind: Service
metadata:
  name: vote
  namespace: voting-app
spec:
  selector:
    app: vote
  type: NodePort
  ports:
  - name: vote
    protocol: TCP
    port: 80
    targetPort: 80
    nodePort: 30000